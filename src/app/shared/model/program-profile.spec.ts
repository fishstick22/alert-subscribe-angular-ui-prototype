import { ProgramProfile } from './program-profile';

describe('Shared/Model: ProgramProfile', () => {
  it('should create an instance', () => {
    expect(new ProgramProfile()).toBeTruthy();
  });
});
